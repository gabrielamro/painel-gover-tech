const lanes = { planned: 'Planejado', planning: 'Em planejamento', development: 'Em desenvolvimento', homologation: 'Em homologação', approved: 'Homologado', billing: 'Aguardando faturamento', completed: 'Faturado', active: 'Em andamento', attention: 'Em atenção', critical: 'Crítica' };
const readSprints = () => { try { return JSON.parse(localStorage.getItem('painelpro-sprints')) || []; } catch { return []; } };
const escText = (value) => String(value).replace(/[&<>"']/g, (char) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#039;' }[char]));
const tone = (health) => health >= 80 ? 'healthy' : health >= 60 ? 'warning' : 'critical';
function renderGover() { if (!location.hash.includes('/dashboard')) return; const main = document.querySelector('.main'); const route = main?.querySelector('.route-content'); if (!main || !route) return; const sprints = readSprints(); if (!sprints.length) return; const tasks = sprints.reduce((sum, sprint) => sum + (sprint.tasks || 0), 0); const deliveries = sprints.reduce((sum, sprint) => sum + (sprint.deliveries || 0), 0); const blocked = sprints.reduce((sum, sprint) => sum + (sprint.blocked || 0), 0); const progress = Math.round(sprints.reduce((sum, sprint) => sum + (sprint.progress || 0), 0) / sprints.length); const attention = sprints.filter((sprint) => sprint.health < 70 || sprint.blocked > 0); const dashboardNavLabel = document.querySelector('.nav-item[href="#/dashboard"] span'); if (dashboardNavLabel && dashboardNavLabel.textContent !== 'Dashboard Gover') dashboardNavLabel.textContent = 'Dashboard Gover'; main.querySelector('h1').textContent = 'Dashboard Gover'; main.querySelector('.breadcrumb').innerHTML = 'Visão geral <span>/</span> Gestão executiva'; main.querySelector('.subhead').textContent = 'Acompanhe os sistemas, entregas e riscos do portfólio.'; route.outerHTML = `<div class="gover-dashboard"><div class="gover-kpis"><div class="gover-kpi"><span class="kpi-icon blue">◒</span><div><small>Total de Sprints</small><strong>${sprints.length}</strong><em>Portfólio atual</em></div></div><div class="gover-kpi"><span class="kpi-icon purple">▣</span><div><small>Tasks no Portfólio</small><strong>${tasks}</strong><em>${blocked} bloqueadas</em></div></div><div class="gover-kpi"><span class="kpi-icon green">✓</span><div><small>Entregas registradas</small><strong>${deliveries}</strong><em>Atualizadas pelas Sprints</em></div></div><div class="gover-kpi"><span class="kpi-icon orange">!</span><div><small>Itens em atenção</small><strong>${attention.length}</strong><em>Exigem acompanhamento</em></div></div></div><section class="gover-section"><div class="gover-section-title"><h2>Status dos Sistemas</h2><span>${sprints.length} Sprints conectadas ao portfólio</span></div><div class="system-grid">${sprints.map((sprint) => `<article class="system-card"><div class="system-head"><span class="system-avatar ${tone(sprint.health)}">${escText(sprint.project.slice(0, 2).toUpperCase())}</span><div><b>${escText(sprint.project)}</b><small>${sprint.code} · ${escText(sprint.objective)}</small></div><span class="system-health ${tone(sprint.health)}">${sprint.health}</span></div><strong class="system-progress">${sprint.progress}%</strong><div class="progress-bar"><span style="width:${sprint.progress}%"></span></div><div class="system-foot"><span>${sprint.tasks} tasks · ${sprint.blocked} bloqueadas</span><span class="system-status ${tone(sprint.health)}">● ${lanes[sprint.lane]}</span></div></article>`).join('')}</div></section><div class="gover-bottom"><section class="gover-panel"><div class="gover-panel-title"><h2>Progresso Geral dos Projetos</h2><span>Planejado × Real</span></div><div class="gover-progress-summary"><strong>${progress}%</strong><span>avanço geral</span><div class="mini-line"><span style="width:${progress}%"></span></div><small>Planejado 75% <em>Real ${progress}%</em></small></div><div class="gover-bars">${sprints.slice(0, 6).map((sprint) => `<div><span>${escText(sprint.project)}</span><i><b style="width:${sprint.progress}%"></b></i><small>${sprint.progress}%</small></div>`).join('')}</div></section><section class="gover-panel"><div class="gover-panel-title"><h2>Atividades Recentes</h2><a href="#/update">Nova atualização →</a></div><div class="activity-list">${sprints.slice().sort((a,b) => b.deliveries - a.deliveries).slice(0,3).map((sprint) => `<div class="activity"><span class="activity-icon green">✓</span><div><b>Atualização registrada</b><p>${escText(sprint.project)} · ${sprint.code}</p><small>${escText(sprint.objective)}</small></div><time>${sprint.deliveries} ent.</time></div>`).join('')}</div><a class="panel-link" href="#/kanban">Ver operação completa →</a></section><section class="gover-panel"><div class="gover-panel-title"><h2>Alertas e Riscos</h2><a href="#/attention">Ver sala →</a></div><div class="risk-list">${attention.slice().sort((a,b) => a.health - b.health).slice(0,3).map((sprint) => `<div class="risk-item"><span class="risk-icon ${sprint.health < 60 ? 'red' : 'yellow'}">!</span><div><b>${sprint.blocked ? 'Tasks bloqueadas' : 'Progresso abaixo do esperado'}</b><p>${escText(sprint.project)} · ${sprint.code}</p><small>${sprint.blocked} bloqueios · ${sprint.risks} riscos</small></div></div>`).join('') || '<div class="empty-state">Nenhum alerta ativo.</div>'}</div><a class="panel-link" href="#/attention">Ver todos os alertas →</a></section></div></div>`; }
new MutationObserver(renderGover).observe(document.body, { childList: true, subtree: true });
window.addEventListener('hashchange', () => setTimeout(renderGover, 0));
document.addEventListener('DOMContentLoaded', () => setTimeout(renderGover, 200));
setTimeout(renderGover, 50);
setInterval(renderGover, 500);
